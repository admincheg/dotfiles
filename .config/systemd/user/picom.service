[Unit]
Description=Picom daemon
Documentation=man:picom(1)
After=graphical-session.target

[Service]
Type=forking
ExecStart=/usr/bin/picom -CGFfb
Slice=session.slice

[Install]
WantedBy=default.target
